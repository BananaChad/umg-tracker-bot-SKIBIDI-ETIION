const updater = require("../database/queries/guild.js");

module.exports = (client) => {
	//updater.fetchOrCreateGuild();
	console.log("WORK ON FETCHORCREATE!!!");
	console.log(`[READY] ${client.user.tag} is online.`);
};
